## Task 1 — Create the static app shell

built the first static Task Tracker UI with a header, input section, 
filter buttons, hardcoded task list, and summary.
Concepts: JSX, components, props, TypeScript props, list rendering with map.